# 国际化设计方案

## 概述

为 OpenCode Go 的 Flutter 移动端和 Electron 桌面端应用添加国际化支持，实现中英双语切换。

## 需求

| 项目 | 决定 |
|------|------|
| 平台范围 | Flutter + Electron |
| 支持语言 | 中文、英文 |
| 切换方式 | 跟随系统 + 应用内手动切换 |
| 翻译管理 | 内嵌翻译文件 |
| RTL 支持 | 不需要 |

## 技术选型

| 平台 | 技术方案 |
|------|---------|
| Flutter | `flutter_localizations` + `intl` + ARB 文件 |
| Electron | `react-i18next` + JSON 文件 |

## 文件结构

```
opencode-go/
├── apps/
│   ├── app/                          # Flutter
│   │   ├── lib/
│   │   │   ├── l10n/                 # 翻译文件
│   │   │   │   ├── app_en.arb        # 英文
│   │   │   │   └── app_zh.arb        # 中文
│   │   │   ├── providers/
│   │   │   │   └── locale_provider.dart  # 语言状态管理
│   │   │   └── generated/            # 自动生成
│   │   │       └── intl/
│   │   ├── l10n.yaml                 # 代码生成配置
│   │   └── pubspec.yaml              # 添加依赖
│   │
│   └── desktop/                      # Electron
│       ├── src/
│       │   ├── locales/              # 翻译文件
│       │   │   ├── en.json
│       │   │   └── zh.json
│       │   ├── i18n/                 # i18n 模块
│       │   │   └── index.ts          # 初始化配置
│       │   ├── hooks/
│       │   │   └── useLanguage.ts    # 语言切换 Hook
│       │   └── components/
│       │       └── settings/
│       │           └── LanguageSelector.tsx
│       └── package.json              # 添加依赖
```

## Flutter 实现细节

### 依赖配置

```yaml
# pubspec.yaml
dependencies:
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

flutter:
  generate: true
```

```yaml
# l10n.yaml
arb-dir: lib/l10n
template-arb-file: app_zh.arb
output-localization-file: app_localizations.dart
output-class: AppLocalizations
```

### 入口配置

```dart
// main.dart
MaterialApp(
  localizationsDelegates: [
    AppLocalizations.delegate,
    GlobalMaterialLocalizations.delegate,
    GlobalWidgetsLocalizations.delegate,
    GlobalCupertinoLocalizations.delegate,
  ],
  supportedLocales: [
    Locale('zh'),
    Locale('en'),
  ],
  locale: ref.watch(localeProvider),
);
```

### 语言状态管理

```dart
// lib/providers/locale_provider.dart
final localeProvider = StateNotifierProvider<LocaleNotifier, Locale?>((ref) {
  return LocaleNotifier();
});

class LocaleNotifier extends StateNotifier<Locale?> {
  LocaleNotifier() : super(_loadFromPrefs());

  static Locale? _loadFromPrefs() {
    final prefs = SharedPreferences.getInstance();
    final code = prefs.getString('locale');
    return code != null ? Locale(code) : null;
  }

  Future<void> setLocale(Locale? locale) async {
    final prefs = await SharedPreferences.getInstance();
    if (locale == null) {
      await prefs.remove('locale');
    } else {
      await prefs.setString('locale', locale.languageCode);
    }
    state = locale;
  }
}
```

### ARB 文件格式

```json
// lib/l10n/app_zh.arb（模板文件）
{
  "@@locale": "zh",
  "appTitle": "OpenCode Go",
  "connectTitle": "连接配置",
  "connectHostLabel": "主机地址",
  "connectPairingCode": "配对码",
  "connectButton": "连接",
  "homeTitle": "工作区",
  "homeEmptyState": "暂无工作区",
  "chatPlaceholder": "输入消息...",
  "chatSendButton": "发送",
  "chatAttachImage": "添加图片",
  "settingsTitle": "设置",
  "settingsLanguage": "语言",
  "settingsLanguageSystem": "跟随系统",
  "errorConnectionFailed": "连接失败",
  "errorNetworkError": "网络错误",
  "loading": "加载中...",
  "retry": "重试"
}
```

```json
// lib/l10n/app_en.arb
{
  "@@locale": "en",
  "appTitle": "OpenCode Go",
  "connectTitle": "Connection",
  "connectHostLabel": "Host Address",
  "connectPairingCode": "Pairing Code",
  "connectButton": "Connect",
  "homeTitle": "Workspaces",
  "homeEmptyState": "No workspaces",
  "chatPlaceholder": "Type a message...",
  "chatSendButton": "Send",
  "chatAttachImage": "Add Image",
  "settingsTitle": "Settings",
  "settingsLanguage": "Language",
  "settingsLanguageSystem": "System",
  "errorConnectionFailed": "Connection failed",
  "errorNetworkError": "Network error",
  "loading": "Loading...",
  "retry": "Retry"
}
```

### 使用方式

```dart
import 'package:flutter_gen/gen_l10n/app_localizations.dart';

Widget build(BuildContext context) {
  final l10n = AppLocalizations.of(context)!;
  return Text(l10n.homeTitle);
}
```

## Electron 实现细节

### 依赖

```json
// package.json
{
  "dependencies": {
    "react-i18next": "^14.0.0",
    "i18next": "^23.0.0",
    "i18next-browser-languagedetector": "^7.0.0"
  }
}
```

### 初始化配置

```typescript
// src/i18n/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

import en from '../locales/en.json';
import zh from '../locales/zh.json';

const LANGUAGE_KEY = 'opencode-language';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: { en, zh },
    fallbackLng: 'en',
    interpolation: { escapeValue: false },
    detection: {
      order: ['localStorage', 'navigator'],
      lookupLocalStorage: LANGUAGE_KEY,
      caches: ['localStorage'],
      cacheLng: LANGUAGE_KEY,
    },
  });

export default i18n;
```

### 语言切换 Hook

```typescript
// src/hooks/useLanguage.ts
import { useTranslation } from 'react-i18next';

type Language = 'zh' | 'en' | 'system';

export function useLanguage() {
  const { i18n } = useTranslation();

  const currentLanguage: Language =
    localStorage.getItem('opencode-language') === null
      ? 'system'
      : (i18n.language as 'zh' | 'en');

  const setLanguage = (lang: Language) => {
    if (lang === 'system') {
      localStorage.removeItem('opencode-language');
      const systemLang = navigator.language.startsWith('zh') ? 'zh' : 'en';
      i18n.changeLanguage(systemLang);
    } else {
      i18n.changeLanguage(lang);
    }
  };

  return { currentLanguage, setLanguage };
}
```

### JSON 翻译文件

```json
// src/locales/zh.json
{
  "app": {
    "title": "OpenCode Go"
  },
  "connect": {
    "title": "连接配置",
    "hostLabel": "主机地址",
    "pairingCode": "配对码",
    "connectButton": "连接"
  },
  "home": {
    "title": "工作区",
    "emptyState": "暂无工作区"
  },
  "chat": {
    "placeholder": "输入消息...",
    "send": "发送",
    "attachImage": "添加图片"
  },
  "settings": {
    "title": "设置",
    "language": "语言",
    "languageSystem": "跟随系统"
  },
  "error": {
    "connectionFailed": "连接失败",
    "networkError": "网络错误"
  },
  "common": {
    "loading": "加载中...",
    "retry": "重试"
  }
}
```

```json
// src/locales/en.json
{
  "app": {
    "title": "OpenCode Go"
  },
  "connect": {
    "title": "Connection",
    "hostLabel": "Host Address",
    "pairingCode": "Pairing Code",
    "connectButton": "Connect"
  },
  "home": {
    "title": "Workspaces",
    "emptyState": "No workspaces"
  },
  "chat": {
    "placeholder": "Type a message...",
    "send": "Send",
    "attachImage": "Add Image"
  },
  "settings": {
    "title": "Settings",
    "language": "Language",
    "languageSystem": "System"
  },
  "error": {
    "connectionFailed": "Connection failed",
    "networkError": "Network error"
  },
  "common": {
    "loading": "Loading...",
    "retry": "Retry"
  }
}
```

### 使用方式

```tsx
import { useTranslation } from 'react-i18next';

function SettingsPanel() {
  const { t } = useTranslation();

  return (
    <div>
      <h2>{t('settings.title')}</h2>
      <LanguageSelector />
    </div>
  );
}
```

## 语言切换 UI

### Flutter

在设置页面添加语言选择器：

```dart
// lib/widgets/settings/language_selector.dart
class LanguageSelector extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final locale = ref.watch(localeProvider);
    final l10n = AppLocalizations.of(context)!;

    return SegmentedButton<Locale?>(
      segments: [
        ButtonSegment(
          value: null,
          label: Text(l10n.settingsLanguageSystem),
        ),
        ButtonSegment(
          value: Locale('zh'),
          label: Text('中文'),
        ),
        ButtonSegment(
          value: Locale('en'),
          label: Text('English'),
        ),
      ],
      selected: {locale},
      onSelectionChanged: (set) {
        ref.read(localeProvider.notifier).setLocale(set.first);
      },
    );
  }
}
```

### Electron

在 `SettingsPanel` 中添加语言选择器：

```tsx
// src/components/settings/LanguageSelector.tsx
import { useTranslation } from 'react-i18next';
import { useLanguage } from '@/hooks/useLanguage';

export function LanguageSelector() {
  const { t } = useTranslation();
  const { currentLanguage, setLanguage } = useLanguage();

  return (
    <Select value={currentLanguage} onValueChange={setLanguage}>
      <SelectTrigger>
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        <SelectItem value="system">
          {t('settings.languageSystem')}
        </SelectItem>
        <SelectItem value="zh">中文</SelectItem>
        <SelectItem value="en">English</SelectItem>
      </SelectContent>
    </Select>
  );
}
```

## 实施步骤

1. **Flutter**
   - 添加依赖，配置 `l10n.yaml`
   - 创建 ARB 翻译文件
   - 运行代码生成
   - 创建 `LocaleProvider`
   - 修改 `main.dart` 配置 delegates
   - 逐个替换硬编码字符串
   - 添加语言切换 UI

2. **Electron**
   - 添加依赖
   - 创建 i18n 初始化模块
   - 创建 JSON 翻译文件
   - 创建 `useLanguage` Hook
   - 在入口引入 i18n 模块
   - 逐个替换硬编码字符串
   - 添加语言切换 UI

## 注意事项

- Flutter 使用中文（`app_zh.arb`）作为模板文件，确保中文翻译完整
- Electron 语言检测优先级：localStorage > 浏览器语言
- 两个平台的语言偏好存储 key 不冲突（Flutter: `locale`, Electron: `opencode-language`）
- 翻译键使用 camelCase 命名，保持一致性